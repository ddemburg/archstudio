# ArchStudio — QA-PROGRESS
Generated by Sentry Run 1 | 2026-03-28

---

## Section A: Bugs

### 🔴 Critical

| # | Location | Issue | Severity |
|---|----------|-------|----------|
| A1 | `TaskModal` line ~1384 | **Sub-component `FR` defined inside render function.** `const FR=({label,children})=>...` is recreated every render, causing remounts and focus/state loss on every keystroke in the task modal. Violates critical rule. | Critical |
| A2 | `TasksTab` line ~1952 | **Sub-component `TaskRow` defined inside render function.** `const TaskRow=({task,depth=0})=>...` is defined inside `TasksTab`. Recursive component re-instantiated every render — expand/collapse state is lost, child re-renders are uncontrolled. Violates critical rule. | Critical |

### 🟠 High

| # | Location | Issue | Severity |
|---|----------|-------|----------|
| A3 | `MeetingScheduler` line 688 | `navigator.clipboard.writeText(bookingLink).then(...)` — no `.catch()`. If clipboard access is denied (non-HTTPS, user blocked), the UI still shows "Copied!" for 2s — false positive feedback. | High |
| A4 | `FlowView` lines 1623–1636 | Layout algorithm (`taskMap`, `depth`, `getDepth`, `cols`) runs unconditionally on every render — no `useMemo`. O(n²) for large task graphs. Causes visible lag when hovering SVG nodes. | High |
| A5 | `App` line 3962 | `currentUser = db.partners.find(p=>p.email===loggedEmail) \|\| db.partners[0]`. If `db.partners` is empty on initial load, `currentUser` is `undefined`. All downstream `currentUser?.id` calls silently return `undefined`, breaking permission checks. | High |

### 🟡 Medium

| # | Location | Issue | Severity |
|---|----------|-------|----------|
| A6 | `ProjectDetail` line 2470 | Phone validation regex `/^[0-9+\-\s()]{7,15}$/` allows invalid strings like `++++++++++`. Low real-world risk but should be tightened. | Medium |
| A7 | `ClientPortal` lines 2981–2983 | `simulateUpload` is a placeholder (`setTimeout` + fake status change, no actual file upload). The "Upload File" button in client portal doesn't call `sbUploadFile`. Clients believe they uploaded, but nothing is stored. | Medium |
| A8 | `CashflowChart` lines ~2169–2282 | `byMonth`, `months`, `withCum`, `maxAbs` are all recomputed on every render with no `useMemo`. On projects with many milestones this causes visible animation stutter. | Medium |

### 🔵 Low

| # | Location | Issue | Severity |
|---|----------|-------|----------|
| A9 | `MeetingScheduler` line 688 | `copy()` function has no debounce/guard — rapid clicks fire multiple `clipboard.writeText` calls. | Low |
| A10 | `TaskModal` line ~1477 | `setSaved(true);setTimeout(()=>setSaved(false),2000)` inside `save()` — no cleanup on unmount. If modal closes in under 2s, timer fires on unmounted component. Low risk (React 18 silent), but a warning source. | Low |

---

## Section B: UI Gaps vs branding.md

### 🔴 Critical

| # | Location | Issue | Severity |
|---|----------|-------|----------|
| B1 | Global CSS (lines 14–15) | **Entire app color system mismatches branding.** Current: `background:#F5F4F0`, `color:#1A1A1A`, `--green:#3D7A5A`, `--gold:#B45309`. Branding specifies dark steel palette (`--color-steel-900:#0f1318` etc.) and gold accent (`--color-gold-400:#c9983a`). App is ~100% off-brand. | Critical |
| B2 | Everywhere | **Zero glassmorphism applied.** 0 instances of `backdrop-filter` in the entire file. Branding mandates glass cards, glass buttons, glass sidebar active state throughout. | Critical |

### 🟠 High

| # | Location | Issue | Severity |
|---|----------|-------|----------|
| B3 | Sidebar (`aside`, line 4051–4079) | Sidebar background is `#1C1C1E` — close to dark but wrong. Active nav state uses `rgba(61,122,90,.25)` (green-tinted), should be gold glass: `linear-gradient(135deg, rgba(201,152,58,0.25), rgba(201,152,58,0.12))` with gold-300 text. | High |
| B4 | Buttons throughout | Primary buttons use `background:#1A1A1A` or `var(--green)=#3D7A5A`. Branding specifies gold glass style: `linear-gradient(135deg, rgba(201,152,58,0.25), rgba(201,152,58,0.12))` with gold border. | High |
| B5 | `BookingPage` hero gradient | `linear-gradient(160deg, #2A5740 0%, #3D7A5A 55%, #4E9470 100%)` — all greens. Should be steel: `linear-gradient(160deg, #0f1318 0%, #232b3e 55%, #4a5a78 100%)`. | High |
| B6 | Cards/panels throughout | All cards use `background:white` or `background:#F7F6F3` flat style. Should be glass card: `rgba(26,32,48,0.75)` with `backdrop-filter:blur(12px) saturate(1.4)`. | High |
| B7 | Modals | Modal overlays use `background:white` flat style. Should be elevated glass card: `rgba(35,43,62,0.88)` with `backdrop-filter:blur(20px) saturate(1.6)`. | High |

### 🟡 Medium

| # | Location | Issue | Severity |
|---|----------|-------|----------|
| B8 | `ClientPortal` line 3104 | `← לחץ להעלאה` — left arrow in RTL context can confuse. Hebrew "back" is typically `←` (pointing to physical left), but for a call-to-action in RTL the visual direction implies clicking right. Inconsistent with `← חזרה` pattern. | Medium |
| B9 | All cards/panels | No `transition` on hover states — cards don't respond visually on hover. Branding specifies `transition: all 0.2s ease` on interactive elements. | Medium |
| B10 | Status badges (`SBadge`) | Badge colors use hardcoded hex values not aligned with branding semantic tokens (`--color-success:#3d8f5f`, `--color-warning:#b8822a`, `--color-error:#8f3d3d`, `--color-info:#3d668f`). | Medium |
| B11 | File upload drop zone (`TaskModal` line 1532) | `border:2px dashed #E8E6E1` on `background:#F7F6F3` — near-invisible dashed border. Very poor visual affordance. | Medium |

### 🔵 Low

| # | Location | Issue | Severity |
|---|----------|-------|----------|
| B12 | Fonts (line 14 + throughout) | App uses `font-family:'Heebo',sans-serif` (Hebrew web font). Branding spec says `Inter, 'Segoe UI', system-ui`. For a Hebrew RTL app Heebo is appropriate — but there's no font CSS variable. Adding `:root { --font-body: 'Heebo', sans-serif; }` would centralize control. | Low |
| B13 | `FlowView` SVG nodes | Node hover only changes `stroke` color — no transition. Add `style="transition:all 0.12s"` to `<g>` elements. | Low |
| B14 | `CashflowChart` SVG y-axis | `fill="#B0B0B0"` (light gray) on white — poor readability. Use `fill="#4a5a78"` (steel-500). | Low |

---

## Section C: Feature Gaps

| # | Feature | Status |
|---|---------|--------|
| C1 | Real file upload in client portal | `simulateUpload` is a placeholder (see A7). `sbUploadFile` exists but is not wired to the "upload" button in `ClientPortal`. |
| C2 | Typography fluid scale (clamp) | All font sizes are hardcoded px/rem values. No CSS custom properties for fluid text. Branding specifies full `clamp()` scale. |
| C3 | CSS design tokens in `:root` | No branding color tokens defined in `:root`. All colors are hardcoded throughout. |
| C4 | Animation/micro-interactions | `fadeIn` exists for list items but missing from modal open/close, sidebar active transitions, button press feedback. |

---

## Summary

| Category | Critical | High | Medium | Low | Total |
|----------|----------|------|--------|-----|-------|
| A) Bugs | 2 | 3 | 3 | 2 | **10** |
| B) UI gaps | 2 | 5 | 4 | 3 | **14** |
| C) Features | — | — | 4 | — | **4** |
| **Total** | **4** | **8** | **11** | **5** | **28** |

---

## Fix Status

- [ ] A1 — FR sub-component in TaskModal
- [ ] A2 — TaskRow sub-component in TasksTab
- [ ] A3 — clipboard.writeText no .catch()
- [ ] A4 — FlowView layout no useMemo
- [ ] A5 — currentUser undefined when partners empty
- [ ] A6 — Phone validation regex too loose
- [ ] A7 — simulateUpload placeholder (ClientPortal)
- [ ] A8 — CashflowChart no useMemo
- [ ] A9 — copy() no debounce (low)
- [ ] A10 — setSaved timer no cleanup (low)
- [ ] B1 — Global color system mismatch (Run 3)
- [ ] B2 — No glassmorphism (Run 3)
- [ ] B3 — Sidebar active state wrong color (Run 3)
- [ ] B4 — Buttons not gold glass (Run 3)
- [ ] B5 — Hero gradient wrong (Run 3)
- [ ] B6 — Cards not glass (Run 3)
- [ ] B7 — Modals not elevated glass (Run 3)
- [ ] B8 — Arrow direction RTL (medium)
- [ ] B9 — No hover transitions (Run 4)
- [ ] B10 — Status badge semantic colors (Run 3)
- [ ] B11 — Upload zone invisible border (Run 4)
- [ ] B12 — Font CSS variable (Run 4)
- [ ] B13 — FlowView node hover transition (Run 4)
- [ ] B14 — CashflowChart y-axis color (Run 4)
- [ ] C1 — Wire real file upload in ClientPortal
- [ ] C2 — Fluid typography clamp() (Run 4)
- [ ] C3 — CSS design tokens in :root (Run 3)
- [ ] C4 — Micro-interactions/animations (Run 4)
